import { Variants } from 'framer-motion';

/** navHoverTap are the animation variants for the navigation items */
export const navHoverTap: Variants = {
  normal: {
    scale: 1,
  },
  hover: {
    scale: 1.1,
  },
  tap: {
    scale: 0.9,
  },
};
